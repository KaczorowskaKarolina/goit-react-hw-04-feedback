import React from 'react';

const App = () => {
  const appStyles = {
    height: '100vh',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    fontSize: 40,
    color: '#010101'
  };

  return (
    <div style={appStyles}>
      React homework template
    </div>
  );
};

export default App;